# SplitScreenCamera

Qualified name: `manim.camera.mapping\_camera.SplitScreenCamera`

### *class* SplitScreenCamera(left_camera, right_camera, \*\*kwargs)

Bases: [`OldMultiCamera`](manim.camera.mapping_camera.OldMultiCamera.md#manim.camera.mapping_camera.OldMultiCamera)

Initializes a split screen camera setup with two side-by-side cameras.

* **Parameters:**
  * **left_camera** ([*Camera*](manim.camera.camera.Camera.md#manim.camera.camera.Camera))
  * **right_camera** ([*Camera*](manim.camera.camera.Camera.md#manim.camera.camera.Camera))
  * **kwargs** (*dict*)

### Methods

### Attributes

| `background_color`   |    |
|----------------------|----|
| `background_opacity` |    |
